apiVersion
kind: Ingress
metadata: 
  name: dashboard-ingress
  namespaces: kubernetes-dashboard
spec:
  rules:
  - host: dashboard.com
    http:
      paths:
      - backend:
          serviceName: kubernetes-dashboard
          servicePort: 80